#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the integer1 to be multiplied
 * @b: the integer2 to be multiplied
 *
 * Return: results to be multiplied
 */
int mul(int a, int b)
{
	int results;

	results = a * b;
	return (results);
}
